```
创建时间：20250521
状态：待定
```

# 默认方式调用

> 注：模拟器和预览器无法测试，用真机调试

```tsx
				// src/main/ets/pages/SystemPage.ets
				// 右边
        Image($r('app.media.btn_simple_person_scanning_2x'))
          .width(18)
          .height(18)
          .onClick(() => {
            // 定义扫码参数options
            let options: scanBarcode.ScanOptions = {
              scanTypes: [scanCore.ScanType.ALL],
              enableMultiMode: true,
              enableAlbum: true
            };
            // 启动扫码，拉起扫码界面
            scanBarcode.startScanForResult(getContext(this), options,
              (error: BusinessError, result: scanBarcode.ScanResult) => {
                if (error) {
                  hilog.error(0x0001, '[Scan CPSample]',
                    `Failed to get ScanResult by callback with options. Code: ${error.code}, message: ${error.message}`);
                  return;
                }
                hilog.info(0x0001, '[Scan CPSample]', `Succeeded in getting ScanResult by callback with options, result is ${JSON.stringify(result)}`);
                // 扫码成功后解析结果并调用登录接口
                if (result && result.originalValue) {
                  const qrCodeData = result.originalValue; // 扫码结果（例如："login:token:pcDeviceId"）
                  this.handleScanResult(qrCodeData); // 调用处理函数
                }
              })
          })
```



# 自定义扫码

> 需求：样式等内容符合UI定义
>
> - ##### isScanType:'scan_login' // 扫码类型：登录
>
>   
>
> ![image-20250527172634610](./assets/image-20250527172634610.png)
